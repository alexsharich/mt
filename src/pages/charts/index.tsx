import { getLayoutWithSidebar } from '@/Components/WithSideBarLayout/WithSideBarLayout'

const Charts = () => {
  return <div>Charts</div>
}

Charts.getLayout = getLayoutWithSidebar

export default Charts
